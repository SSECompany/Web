# Base environment file
# Create-react-app sẽ tự động load .env.development khi chạy npm start
# và .env.production khi chạy npm run build

# Nếu muốn override, có thể thêm biến ở đây
# Nhưng khuyến nghị: sử dụng .env.development và .env.production
